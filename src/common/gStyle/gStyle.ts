import {StyleSheet} from 'react-native';

const gStyles = StyleSheet.create({
  main: {
    flex: 1,
    padding: 20,
    paddingTop: 60,
    borderColor: '#55423d',
  },
  title: {
    fontSize: 25,
    color: '#fffffe',
    fontFamily: 'kt-bold',
    textAlign: 'center',
    marginTop: 25,
  },
});
